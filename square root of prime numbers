class Solution:
    def isPrime(self, n):
        # code here
        if n<=1:
            return False
        sqrt=int(n**0.5)
        for i in range(2,sqrt+1):
            if n%i==0:
                return False
        return True
       
